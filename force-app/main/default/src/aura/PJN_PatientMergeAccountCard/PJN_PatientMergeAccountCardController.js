({
    handleAccountSelection: function(component, event, helper) {
        helper.handleAccountSelection(component, event, helper);
    },
    openAccount: function(component, event, helper) {
        helper.openAccount(component, event, helper);
    }
});