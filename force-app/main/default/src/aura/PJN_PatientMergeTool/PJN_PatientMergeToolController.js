({
    handleInit : function(component, event, helper) {
        helper.handleInit(component, event, helper);
    },
    handlePrevious: function(component, event, helper) {
        helper.handlePrevious(component, event, helper);
    },
    handleNext: function(component, event, helper) {
        helper.handleNext(component, event, helper);
    },
    handleSelectAccount : function(component, event, helper) {
        helper.handleSelectAccount(component, event, helper);
    },
    handleSelectCarePlan : function(component, event, helper) {
        helper.handleSelectCarePlan(component, event, helper);
    },
    handleSelectChildRecord : function(component, event, helper) {
        helper.handleSelectChildRecord(component, event, helper);
    },
    handleSelectPrimary : function(component, event, helper) {
        helper.handleSelectPrimary(component, event, helper);
    },
    closeModal : function(component, event, helper) {
        helper.hideModal(component, event, helper);
    },
    hideProgressButtons : function(component, event, helper) {
        helper.hideProgressButtons(component, event, helper);
    },
    updateCarePlanList : function(component, event, helper) {
        helper.updateCarePlanList(component, event, helper);
    },
    setMergedCarePlanIds : function(component, event, helper) {
        helper.setMergedCarePlanIds(component, event, helper);
    },
});